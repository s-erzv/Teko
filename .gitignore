# Foundry (kontrak — repo root)
out/
cache/
broadcast/
.env

# Bot Node.js hidup di bot/, punya .gitignore sendiri (lihat bot/.gitignore)

# Logs / OS
*.log
.DS_Store

# Output tool graphify (lokal, gak perlu di-commit)
graphify-out/

# Next.js (web/)
web/.next/
web/out/
node_modules/
.gstack/
